server {
    listen 80;
    server_name auth.little.bloomyindev.me;
    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl;
    server_name auth.little.bloomyindev.me;

    ssl_certificate     /etc/nginx/certs/auth.little.bloomyindev.me.crt;
    ssl_certificate_key /etc/nginx/certs/auth.little.bloomyindev.me.key;

    location / {
        proxy_pass http://keycloak_auth:8080;

        proxy_set_header Host               $host;
        proxy_set_header X-Real-IP          $remote_addr;
        proxy_set_header X-Forwarded-For    $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto  $scheme;
        proxy_set_header X-Forwarded-Host   $host;
        proxy_set_header X-Forwarded-Port   $server_port;

        # Nécessaire pour les redirects OIDC
        proxy_buffer_size         128k;
        proxy_buffers             4 256k;
        proxy_busy_buffers_size   256k;
    }
}